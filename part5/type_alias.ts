{
    type FooType = [string, number, boolean];
    // let data: FooType = ['abc', 'xyz', true];
    let data: FooType = ['abc', 123, true];

    type HogeType = number | string;
    let data1: HogeType = 123;
}